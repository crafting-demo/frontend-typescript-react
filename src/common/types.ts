export enum ActionType {
  Echo = "Echo",
  Read = "Read",
  Write = "Write",
  Call = "Call",
}

export enum Topic {
  React = "queue-react",
  Golang = "queue-golang",
  Express = "queue-express",
  Ruby = "queue-ruby",
  Kotlin = "queue-kotlin",
  Python = "queue-python",
}

export enum Dependency {
  MySQL = "mysql",
  Postgres = "postgres",
  MongoDB = "mongodb",
  DynamoDB = "dynamodb",
  Redis = "redis",
}
