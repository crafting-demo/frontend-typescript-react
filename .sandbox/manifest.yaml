hooks:
  post-checkout:
    cmd: yarn install
  build:
    cmd: yarn build

daemons:
  app:
    run:
      cmd: yarn start
      env:
        - HOST=0.0.0.0
        - PORT=3000
        - REACT_APP_DNS_SUFFIX=$SANDBOX_ENDPOINT_DNS_SUFFIX
